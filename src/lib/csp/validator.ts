/**
 * Punctul public de intrare al validatorului CSP descris în documentație.
 * Motorul rămâne separat pentru compatibilitate și testare izolată.
 */
export {
  applyOperator,
  resolveField,
  validateConfiguration,
} from "./engine";
